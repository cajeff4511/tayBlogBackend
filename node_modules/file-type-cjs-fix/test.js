const fs = require('fs')
const path = require('path')
const fileType = require('./file-type')
;(async function exec() {
  const filePath = path.resolve('./pepe.png')
  const buff = await fs.promises.readFile(filePath)
  const type = await fileType.fromBuffer(buff)
  console.log(type)
})()
